import styled from "styled-components";

const ErrorContainer = styled.div`
  border: 2px solid #ff4a4a;
  padding: 2rem;
  border-radius: 2px;
  color: #ff4a4a;
`;

const Details = styled.details`
  white-space: pre-wrap;
`;

export { ErrorContainer, Details };
