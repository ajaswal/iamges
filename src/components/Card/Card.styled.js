import styled from "styled-components";

const ImageContainer = styled.div`
  display: block;
  flex-direction: row;
  margin: 10px;
  border: 5px solid #fff;
  
`;

const Message = styled.p`
  color: #000;
`;

export { ImageContainer, Message };
